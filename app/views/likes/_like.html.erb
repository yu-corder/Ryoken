<% if Like.find_by(user_id: current_account.id, cookpost_id: cookpost.id) %>
  <%= link_to 'いいね取り消し', like_path(cookpost.id), method: :delete, remote: true %>
<% else %>
  <%= link_to 'いいね', like_path(cookpost.id), method: :post, remote: true %>
<% end %>
<%= cookpost.likes.length %>